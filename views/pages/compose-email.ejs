<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Gửi email</title>
  <link rel="stylesheet" href="styles.css">
</head>
<style>
    body {
  font-family: Arial, sans-serif;
  margin: 0;
  padding: 0;
}

.container {
  width: 1000px;
  height: 500px;
  margin: 0 auto;
  display: flex;
  justify-content: center;
  align-items: center;
  background-color: #f1f1f1;
  box-shadow: 0px 0px 5px rgba(0, 0, 0, 0.1);
}

.title {
  font-size: 24px;
  font-weight: bold;
  margin-bottom: 20px;
}

.form-group {
  margin-bottom: 20px;
}

label {
  display: block;
  font-weight: bold;
  margin-bottom: 5px;
}

input[type="text"],
textarea {
  width: 100%;
  padding: 10px;
  font-size: 14px;
  border: 1px solid #ccc;
  border-radius: 4px;
}

input[type="file"] {
  display: block;
  margin-top: 5px;
}

.btn-submit {
  padding: 10px 20px;
  font-size: 16px;
  background-color: #4CAF50;
  color: #fff;
  border: none;
  border-radius: 4px;
  cursor: pointer;
}

.container {
  background-color: #f2f8f8;
  padding: 30px;
}

.title {
  color: #145c9e;
}

.form-group {
  margin-bottom: 30px;
}

label {
  color: #145c9e;
}

input[type="text"],
textarea {
  background-color: #e8f4f4;
  border-color: #145c9e;
  color: #145c9e;
}

input[type="file"] {
  background-color: #e8f4f4;
  border-color: #145c9e;
  color: #145c9e;
}

.btn-submit {
  background-color: #145c9e;
  color: #fff;
}
</style>
<%- include('../partials/header') %>

<body>
  <div class="container">
    <form action="/api/create" method="POST"  enctype="multipart/form-data">
      <div class="form-group">
        <label for="recipient">Người nhận:</label>
        <select id="recipient" name="recipient">
            <%listuser.forEach((item)=>{%>
          <option value="<%=item.id%>"><%=item.fullname%></option>
          <%})%>
        </select>
      </div>
      <div class="form-group">
        <label for="subject">Tiêu đề:</label>
        <input type="text" id="subject" name="tieude">
      </div>
      <div class="form-group">
        <label for="message">Nội dung:</label>
        <textarea id="message" name="noidung"></textarea>
      </div>
      <div class="form-group">
        <label for="attachment">Đính kèm file:</label>
        <input type="file" id="attachment" name="tenfile">
      </div>
      <button type="submit" class="btn-submit">Gửi</button>
    </form>
  </div>
</body>
</html>